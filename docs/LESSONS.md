# 오답노트 (Lessons Ledger)

> 목적: **같은 결함이 두 번 다시 게이트를 통과할 수 없게 만든다.**
> 규칙: 결함이 새로 발견되면 즉시 이 원장에 등록하고, 재발 방지 가드(검증 절차·체크리스트·테스트)를 함께 만든다.
> 항목은 삭제하지 않는다. 운영 절차는 `.claude/skills/lessons-audit-gate/SKILL.md` 참조.

| No | 발생일 | 오류 (무엇을 틀렸나) | 원인 | 교정 | 재발 방지 규칙(가드) |
|---|---|---|---|---|---|
| L001 | 2026-07-22 | K-SURE "연 10회 무료 신용조사"를 현행 제도로 인용 | 2019년 과거 자료를 검증 없이 사용 | 재검증으로 현행 확인(신규·리턴 3건 무료, 33,000/49,500원) 후 문서 정정 | 공공기관 혜택·수수료 수치 하드코딩 금지 — Policy Registry에 `verified_at`과 함께 저장, UI에는 조회 링크만 노출 (ARCHITECTURE.md §8.2) |
| L002 | 2026-07-22 | DLP 무료 CBP B/L 덤프를 Phase 1 데이터 소스로 추천 | 소스 실존 여부 미확인 상태로 로드맵 반영 | FOIA 기각(2023-04-28) 확인 — 무료 경로 삭제, 유료 구독/상용 벤더로 대체 | 데이터 소스는 실존·접근 가능성·권리 조건이 CONFIRMED된 뒤에만 로드맵 등재. 미검증 소스는 UNKNOWN 표기 (ARCHITECTURE.md §7.2) |
| L003 | 2026-07-23 | 구글시트 생성 시 숫자 나열("1, 2, 10")이 날짜(2001-02-10)로 자동 변환됨 | 한국어 로캘의 날짜 자동 인식 미고려 | 구분자를 `·`로 변경해 재생성, 재읽기로 확인 | 외부 시스템에 쓴 데이터는 반드시 다시 읽어 원본과 대조한다(쓰기≠검증). 숫자 나열 필드는 자동 형변환 방지 포맷 사용 |
| L004 | 2026-07-23 | 모니터링 시트에서 아키텍처 구성요소·수집소스 인벤토리 등 대량 누락 | "전체 포함" 요청을 원문서 전수 대조 없이 기억으로 요약 작성 | 두 설계 문서 전문 재독 후 9탭 워크북으로 전면 재작성 | 전수 기록 요청은 원본 문서를 다시 읽고 항목 단위로 대조하며 작성한다. 요약본을 전수 기록으로 제출하지 않는다 |
| L005 | 2026-07-23 | 문서 섹션 번호 4건 부정확 인용(§4→§2.1/§3, §9→§2.3, §5.3→§5.2, §2.1→§2.0) | 문서를 다시 열지 않고 기억으로 § 번호 인용 | 검증 에이전트 대조로 발견·정정 | 문서 참조(§ 번호·수치)는 인용 시점에 원문을 열어 확인한다. 기억 인용 금지 |
| L006 | 2026-07-23 | 대조군 실험(100 vs 100)·응답률 5~15%를 문서에 있는 것처럼 기재 | 세션 합의와 문서 반영 상태를 구분하지 않음 | "세션 설계·문서 미반영"으로 명시 수정, 문서화를 별도 과제로 등재 | 모든 주장에 상태 라벨을 붙인다: 문서 확정 / 세션 합의(미반영) / 가정 / 검증필요 |
| L007 | 2026-07-23 | 개발현황 목록을 문제 단위 22건으로만 제공 — 작업 단위 분해(WBS) 부재 | 문제 관리 관점과 개발 진행 관점을 구분하지 않음 | 작업분해(WBS) 77건 탭 추가 — DDL·서비스·화면·운영 단위 전수 분해 | 개발 모니터링 산출물에는 문제 목록과 작업 분해를 모두 포함하고, "전체"의 단위(문제/작업/문서)를 산출 전에 정의한다 |
| L008 | 2026-07-23 | 모니터링 시트를 버전마다 새 파일로 생성해 드라이브에 구버전 파일 누적 | 세션의 Google Drive 커넥터에 수정(update) 도구가 없음을 사용자에게 알리지 않고 새 파일 생성으로 우회 | v3를 단일 기준 시트로 선언, 구버전 삭제 안내, 행 단위 수정 경로(Zapier Google Sheets) 확인 | 외부 산출물을 갱신할 때 수정이 불가한 도구 제약이 있으면 새 파일 생성 전에 사용자에게 제약과 대안을 먼저 알린다 |
| L009 | 2026-07-25 | 바이어 화면(A-003)에 Math.random 수입이력·성장률·RFM·갱신일과 final_score÷20 파생 수입액을 실데이터처럼 표시 | 데모용 목업 값을 실서비스 화면에 그대로 두고 데이터 정책(원본에 없는 값 미생성)을 강제하는 장치가 없었음 | 합성값 전부 제거, null → '자료 내 확인 불가' 표시, 뷰모델을 순수 함수로 분리 | 화면 값은 API·CSV 원본 필드에서만 파생한다. 가드: `tests/buyerViewModel.test.mjs` (null 필드·$M 파생값 금지·동일 입력 10회 결정성 검증) |
| L010 | 2026-07-25 | has_contact=true 하나로 '신뢰도 높음/연락처 확인됨/검증 완료'를 표시하고 검증일을 오늘 날짜로 생성 | 연락처 보유·형식검증·소유검증·거래출처·신용을 구분하는 상태 모델 부재 | contact_status/trade_status/credit_status 3축 분리, 검증 절차 없는 상태값은 부여 안 함 | 검증 관련 표시는 상태 enum에서만 파생하고, 절차 없이 상위 상태(format_validated 이상)를 부여하지 않는다. 가드: `tests/buyerViewModel.test.mjs` 상태 분리 테스트 |
| L011 | 2026-07-25 | 수익성 계산이 제품원가·결제수수료·보험료·통관비를 누락해 이익을 과대계상 (동일 입력에서 $89,000 vs 정상 $26,000) | 매출-일부비용 약식 계산을 검증 테스트 없이 사용 | 공용 `computeProfitability` 모듈로 정상식(총매출−총비용) 적용, 구식 대비 차액을 테스트로 고정 | 금액 계산식은 공용 모듈 1곳에서만 정의하고 단위테스트로 고정한다. 가드: `tests/profitability.test.mjs` (구식 과대계상 비교 테스트 포함) |
| L012 | 2026-07-25 | 로그인 화면에 98% 매칭 정확도·50K+ 바이어 등 근거 없는 수치, 배너에 240만 건·실시간 연동 표기 | 마케팅 문구를 실측 데이터와 분리 관리하지 않음 | 수치를 `/v1/demo/summary` 실측 집계로 대체, 정확도 수치는 검증표본 확보 전 사용 금지 | 화면의 모든 정량 주장에는 산출 근거(API/CSV)가 있어야 하며, 근거 없는 수치는 표기하지 않는다 (L001·L006 연장) |
| L013 | 2026-07-27 | Toss DONE 웹훅 재전송 시 동일 orderId로 크레딧 중복 적립(100→110→120), 체크아웃 orderId가 `{user_id}-{product}-{item}` 고정이라 동일 패키지 재구매 시 Toss orderId 충돌 | 웹훅 이행 경로에 order_id 멱등 키 부재, 체크아웃에 결제 시도별 nonce 부재 | `fulfill_payment_once()`가 결제 잠금 하에 확인→적용→기록, 체크아웃 orderId에 nonce 추가(`{user_id}.{type}.{item}.{nonce}`, 레거시 하이픈 형식 파싱 유지) — PR #86 | 결제 부수효과(크레딧 적립·플랜 변경)는 order_id 멱등 게이트를 통해서만 적용한다. 가드: `services/p1-export-fit-api/tests/test_payment_webhook_idempotency.py` 재전송 재현 테스트 |
| L014 | 2026-07-28 | PR #86이 "중복 적립 수정 완료"를 주장했으나 운영 구성(render.yaml `UVICORN_WORKERS: "4"`)에서는 `threading.Lock`이 프로세스 간 무효 — 2프로세스 실험 5/5회 양쪽 모두 적립(검증 PARTIAL 판정) | 코드의 단일 프로세스 가정과 배포 구성(멀티워커)을 대조하지 않고 단일 프로세스에서만 검증 | workers 1로 완화, 근본 해결(DB unique index on order_id)은 별도 과제 | 동시성 가드의 완결 주장 전 배포 구성(워커 수·프로세스 모델)과 반드시 대조한다. 인프로세스 잠금은 멀티워커 배포에서 멱등 가드가 아니다 |
| L015 | 2026-07-28 | 미지 패키지 orderId의 서명된 DONE 웹훅에 400 응답 — PG에서 돈은 빠졌는데 원장 기록 0, Toss 재전송 소진 후 영구 무기록 유실 | 재시도로 해결 불가능한 실패에 비-2xx 응답 사용(웹훅 재전송 시맨틱 미고려) | NEEDS_REVIEW 상태로 기록 후 200 반환으로 변경 | 웹훅 비-2xx는 재시도로 해결 가능한 실패에만 사용. 식별 불가 결제는 반드시 기록(NEEDS_REVIEW)하고 200. 가드: 미지 패키지 웹훅 테스트 |
| L016 | 2026-07-28 | 결제 원장 `_save`가 제자리 덮어쓰기(tmp+rename 없음)이고 `_load`가 손상 시 빈 원장으로 fail-open — 찢어진 쓰기 한 번에 전체 orderId가 재적립 가능 상태로 복귀 | 금전 원장에 일반 파일 쓰기 패턴 사용, 손상 시나리오 미고려 | `os.replace` 원자적 쓰기로 교체(payment/credit store) | 금전 상태 파일 쓰기는 원자적 교체(tmp+`os.replace`) 필수. 손상 시 빈 데이터 취급 금지 |
| L017 | 2026-07-28 | 웹훅이 `totalAmount`를 기록만 하고 패키지 가격과 대조하지 않음 — 20,000원 결제 웹훅으로 160,000원 large 패키지(100크레딧) 적립 재현 | 서명 검증을 금액 검증으로 오인(서명은 발신자만 증명, 주문 내용은 미증명) | 이행 전 totalAmount를 CREDIT_PACKAGES/PLAN_PRICES 가격과 대조, 불일치는 NEEDS_REVIEW | 웹훅 서명은 발신자 증명일 뿐 — 이행 전 금액·상품 대조 필수. 가드: 금액 불일치 웹훅 테스트 |
| L018 | 2026-07-28 | 웹훅 핸들러가 Toss 페이로드를 최상위 평면 구조(`status`/`orderId`/`totalAmount`가 루트)로 가정 — 공식 문서상 `PAYMENT_STATUS_CHANGED`는 `{eventType, createdAt, data:{...}}` 래핑이라 실제 웹훅이 전량 무시될 구조(검증 CONFIRMED) | 외부 연동 스키마를 공식 문서 대조 없이 자체 테스트 페이로드 기준으로 구현 | 핸들러가 래핑(`data` 추출)·평면 양쪽을 파싱, eventType 분기 추가 | 외부 제공자 연동(웹훅·API)은 공식 문서/실페이로드로 스키마 확인 후 구현하고, 문서 기반 페이로드 재현 테스트를 가드로 둔다. 서명 헤더 등 미확정 사항은 실전송 로그 확인 전 구현 확정 금지 |
| L019 | 2026-07-28 | 유료 상품 2종이 전부 무료 획득 가능 — `POST /v1/credits/charge`는 임의 금액 자가 충전, `POST /v1/subscription/change`는 무결제 Advanced 자가 승급(실증: 200 응답, 결제 원장 0행) | 결제로만 도달해야 할 상태 변경 라우트에 인증(get_current_user)만 걸고 권한(require_admin)·결제 게이트를 걸지 않음 | 두 라우트 모두 require_admin 게이트 적용(기존 ACL 재사용) | 크레딧·플랜 등 과금 대상 상태의 변경 라우트는 결제 이행 경로(웹훅/confirm) 또는 admin 권한을 통해서만 노출한다. 가드: ACL 403 테스트 |
| L020 | 2026-07-28 | 무인 병합 직전, 리뷰는 커밋 a7e7c8f 기준인데 워킹트리에는 미커밋 수정 6개 파일 존재 — 그대로 병합했다면 결함 버전(실웹훅 전량 무시)을 "검증 완료"로 믿고 출고할 뻔함 | 리뷰 대상(커밋 해시)과 병합 대상(브랜치 HEAD+워킹트리)의 동일성을 검증하는 절차 부재 | 병합 전제조건 명문화: 트리 클린 + HEAD==리뷰된 해시 + 인시추 테스트 green + 최종 해시에 대한 명시적 MERGE 판정 | 무인/자동 병합은 반드시 "리뷰어가 판정한 커밋 해시"와 "병합되는 해시"의 일치·클린 트리를 기계적으로 확인한 뒤 수행한다 |
| L021 | 2026-07-28 | L010 가드 테스트가 상태 모델 확장(형식 검증 절차 도입, 717d24e) 뒤 갱신되지 않아 스위트에 상시 RED로 방치 — 실제 회귀와 구분 불가 상태로 발견 | 상태 모델을 확장한 커밋이 그 모델을 고정하는 가드 테스트를 함께 갱신하지 않음 | 가드를 확장 모델에 맞게 갱신하고 승급 배제 조건(estimated 이메일·형식 불량·bare has_contact) 고정 케이스 추가 | 상태 모델·정책을 확장하는 커밋은 해당 가드 테스트를 같은 커밋에서 갱신한다. 상시 RED 가드는 발견 즉시 회귀/스테일을 판정해 해소한다(방치 금지) |
| L022 | 2026-07-28 | 구조(AST) 핀 도입 시 유일한 런타임 성공 경로 테스트(large 패키지 이행)가 "대체됐다"며 삭제됨 — 이행 조회를 다른 카탈로그 항목으로 바꾸는 뮤테이션(160,000원 결제에 10크레딧 적립)이 전체 스위트를 무검출 통과함을 대조 실험으로 확인 | 구조 가드(값의 출처 고정)와 런타임 가드(어느 카탈로그 항목을 읽어 실제로 얼마를 적립하는가)를 대체 관계로 오인 | 런타임 성공 경로 테스트 복원 — 해당 뮤테이션을 유일하게 검출함을 확인 후 병합 | 구조적 가드는 런타임 행위 가드의 보완이지 대체가 아니다. 가드를 교체·삭제할 때는 기존 가드가 잡던 뮤테이션을 새 가드도 잡는지 확인한 뒤에만 삭제한다 |
