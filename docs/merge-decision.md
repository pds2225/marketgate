# 새 통합 폴더를 다시 만든 이유

## 선택지 1. 기존 `merged_workspace_20260416`를 그대로 계속 사용

### 장점

- 이미 한 번 정리된 결과가 있어서 추가 작업량이 적습니다.
- 현재 프론트와 P1 API 연결 작업도 그 폴더를 기준으로 진행됐습니다.
- 새 복사본을 또 만들지 않아도 됩니다.

### 단점

- 이름 자체가 `20260416` 기준이라, 이후 기준본이 바뀌어도 계속 과거 시점처럼 보입니다.
- 기존 병합 시점의 판단과 현재 판단이 섞이기 쉽습니다.
- "왜 이 폴더가 최종 기준본인지"를 나중에 다시 설명해야 합니다.

## 선택지 2. 새 통합 폴더를 다시 만들어 기준본을 새로 세우기

### 장점

- 지금 시점 기준으로 최종 기준본을 명확하게 다시 선언할 수 있습니다.
- 과거 병합본과 새 기준본을 분리할 수 있어 혼선이 줄어듭니다.
- 앞으로 외부 전달, 인수인계, 추가 개발 때 설명이 더 쉽습니다.

### 단점

- 한 번 더 복사/정리 작업이 필요합니다.
- 병합 기준을 문서로 다시 남겨야 합니다.

## 최종 추천

이번 프로젝트 상태에서는 **선택지 2가 더 낫습니다.**

이유는 단순합니다.
지금 루트에는 복사본, 인수인계본, 실험본, 데이터 원본이 많이 섞여 있어서, 과거 병합본을 계속 덧대는 방식보다 **새 기준본을 다시 선포하는 방식**이 더 안전합니다.

## 이번에 실제로 적용한 기준

- 새 기준 폴더: `D:\valueup-mvp\unified_workspace_20260418`
- 포함:
  - `apps/frontend-react`
  - `apps/web-dig-landing`
  - `services/p1-export-fit-api`
  - `services/ml-export-engine`
  - `archive/legacy-export-intelligence`
  - `ops/monitoring`
- 제외:
  - `개발자결과물`
  - `download data`
  - ZIP/PDF/HWP/DOCX 원본
  - 실행 산출물과 캐시
